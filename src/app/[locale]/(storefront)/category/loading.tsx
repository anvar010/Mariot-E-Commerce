/**
 * Category pages render the same filter-rail + card-grid as the shop, so they reuse its
 * skeleton rather than keeping a near-identical copy in step with it.
 */
export { default } from '../shop/loading';
